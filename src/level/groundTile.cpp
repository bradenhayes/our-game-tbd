// GroundTile.cpp
#include "GroundTile.h"

GroundTile::GroundTile(SDL_Texture* texture) {
    this->texture = texture;
}

void GroundTile::draw(SDL_Renderer* renderer, int x, int y) {
    // Draw the tile at the given position
}
